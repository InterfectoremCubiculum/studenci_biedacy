import { ValidateGradePipe } from './validate-grade.pipe';

describe('ValidateGradePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidateGradePipe();
    expect(pipe).toBeTruthy();
  });
});
